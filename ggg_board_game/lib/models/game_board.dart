class GameBoard {
  var board = {};

  void init(int rows, int cols, int nrOfObstacles) {
    for (var col = 0; col < cols; col++) {
      for (var row = 0; row < rows; row++) {
        // 
      }
    }
  }
}
